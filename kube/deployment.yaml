apiVersion: apps/v1
kind: Deployment
metadata:
  name: gbv-botscribe
  labels:
    app: gbv-botscribe
spec:
  replicas: 2
  selector:
    matchLabels:
      app: gbv-botscribe
  template:
    metadata:
      labels:
        app: gbv-botscribe
    spec:
      containers:
      - name: gbv-botscribe
        image: nandanrao/gbv-botscribe:0.0.2
        resources:
          requests:
            cpu: "100m"
            memory: "500Mi"
        ports:
        - containerPort: 80
        envFrom:
        - secretRef:
            name: gbv-bot-envs

